import { CreateForm } from "@/components/create/CreateForm";

export const metadata = {
  title: "Create a Quran page | QuranForBaba",
};

export default function CreatePage() {
  return <CreateForm />;
}
